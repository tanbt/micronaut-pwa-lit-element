task copyClientResources(dependsOn: ':client:build') {
    group = 'build'
    description = 'Copy client resources into server'
}
copyClientResources.doFirst {
    copy {
        from "${project(':client').buildDir}/es5-bundled" // make sure path is correct
        into "${project(':server').buildDir}/resources/public"
    }
}

task assembleBuilds(dependsOn: ['copyClientResources', ':server:shadowJar']) { 
    group = 'build'
    description = 'Build combined server & client JAR'
}

task test {

}

task check {

}